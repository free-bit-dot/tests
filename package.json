{
  "name": "test-labs",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "jest jest",
    "test:e2e": "playwright test playwright",
    "test:all": "npm run test && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0",
    "jest": "^29.0.0",
    "supertest": "^6.3.3"
  },
  "dependencies": {
    "axios": "^1.6.0"
  }
}
